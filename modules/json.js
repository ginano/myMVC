/**
 *@fileoverview  the JSON plugin for parsing the string to json and stringfing the object
 *@author ginano
 *@website http://www.ginano.net
 *@date 20130228 
 */
define('modules/json',[
        'modules/class'
    ],function(Class){
   var JSON=new Class('modules/json',{
       
   });
   return JSON;
});
